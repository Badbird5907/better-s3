name = "cf-worker"
main = "src/index.ts"
compatibility_date = "2024-12-01"
compatibility_flags = ["nodejs_compat"]

# R2 bucket for uploads (configure with your bucket name)
# [[r2_buckets]]
# binding = "R2_BUCKET"
# bucket_name = "your-bucket-name"

[vars]
# NEXTJS_CALLBACK_URL = "https://your-nextjs-app.com"
# SIGNING_SECRET = "your-secure-random-secret-here"  # Must match the secret in Next.js .env
# For development, you can set this in .dev.vars (not committed to git)